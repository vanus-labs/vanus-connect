apiVersion: v1
kind: Service
metadata:
  name: sink-es
  namespace: vance
spec:
  selector:
    app: sink-es
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: sink-es
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: sink-es-config
  namespace: vance
data:
  config.yaml: |-
    "v_port": 8080
    "address": "http://localhost:9200"
    "username": "elastic"
    "password": "elastic"
    "index_name": "vance_test"
    "primary_key": "data.id"
    "insert_mode": "upsert"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sink-es
  namespace: vance
  labels:
    app: sink-es
spec:
  selector:
    matchLabels:
      app: sink-es
  replicas: 1
  template:
    metadata:
      labels:
        app: sink-es
    spec:
      containers:
        - name: sink-es
          image: vancehub/sink-elasticsearch:0.1
          imagePullPolicy: IfNotPresent
          #command: ['sh', '-c', 'sleep 360000']
          env:
            - name: LOG_LEVEL
              value: DEBUG
          volumeMounts:
            - name: sink-es-config
              mountPath: /vance/config
      volumes:
        - name: sink-es-config
          configMap:
            name: sink-es-config